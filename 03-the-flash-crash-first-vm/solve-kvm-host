#!/bin/bash
# Challenge 3 solve — intentionally a no-op.
#
# algo-trader-01 is self-contained to this chapter: nothing in chapters 4-9
# references it, and the host is short on resources, so it does not get
# pre-provisioned or auto-solved. The student builds it by hand through
# tasks 1-5 in the SUSE Virtualization UI; check-kvm-host verifies what they
# built. There is nothing for a Solve button to auto-create here.
#
# algo-trader-01 gets deleted when chapter 4 starts (see
# 04-the-rising-tide-live-migration/setup-kvm-host) to free the resources
# chapter 4's own VMs need.

echo ">>> Nothing to solve for chapter 3 — algo-trader-01 is built by hand via the UI (tasks 1-5)."
exit 0
