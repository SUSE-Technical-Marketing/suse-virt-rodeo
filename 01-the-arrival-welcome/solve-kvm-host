#!/bin/bash
set -euo pipefail

# Nothing to create — this chapter is about exploring the UI and running
# read-only commands. Must be a true no-op: Instruqt's Skip button runs this
# script, and skipping is most likely to happen early, before the cluster
# (up to 90 minutes to bring all 3 Harvester nodes Ready) has finished
# initializing. The old version gated on 3/3 nodes Ready and exited 1
# otherwise, which made Skip fail with an error during exactly that window.
# check-kvm-host still verifies cluster health for students who complete the
# chapter normally instead of skipping it.

echo ">>> Nothing to solve for Chapter 1 — skipping straight to Chapter 2."
